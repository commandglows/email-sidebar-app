name: shipglows_flutter_zoom
description: App-wide Ctrl/Command+wheel zoom for Flutter web and desktop.
version: 0.1.0
repository: https://github.com/commandglows/email-sidebar-app/tree/main/packages/shipglows_flutter_zoom
issue_tracker: https://github.com/commandglows/email-sidebar-app/issues

environment:
  sdk: ^3.11.3
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
