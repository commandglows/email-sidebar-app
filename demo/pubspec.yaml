name: source_sidebar_preview
description: Interactive Flutter Web preview for source_sidebar_flutter.
publish_to: none
version: 0.2.0+2

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  source_sidebar_flutter:
    path: ../packages/source_sidebar_flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
