name: source_sidebar_preview
description: Interactive Flutter Web preview for the shared Sources and Newsletter Studio packages.
publish_to: none
version: 0.4.0+4

environment:
  sdk: ^3.11.0
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  newsletter_studio_flutter:
    path: ../packages/newsletter_studio_flutter
  source_sidebar_flutter:
    path: ../packages/source_sidebar_flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
